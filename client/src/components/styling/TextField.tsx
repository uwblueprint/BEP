import { Theme } from "@material-ui/core/styles";

const outlineTextFieldStyle = (theme: Theme) => ({
  textField: {
    background: theme.palette.primary.dark,
  },
});

export { outlineTextFieldStyle };
